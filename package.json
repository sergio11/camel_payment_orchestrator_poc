{
  "name": "poc-camel",
  "version": "1.0.0",
  "description": "Payment Orchestration Layer - SDD Workflow",
  "scripts": {
    "openspec": "openspec",
    "openspec:validate": "openspec validate --all",
    "openspec:list": "openspec list",
    "openspec:show": "openspec show",
    "openspec:validate:changes": "openspec validate --changes",
    "openspec:validate:specs": "openspec validate --specs",
    "arch:check": "npm run arch:check --prefix backend && npm run arch:check --prefix frontend"
  },
  "devDependencies": {
    "@fission-ai/openspec": "1.3.1"
  }
}