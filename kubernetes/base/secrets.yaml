# WARNING: This is a template with placeholder values.
# Real secrets must be injected via ExternalSecrets, SealedSecrets, or Vault.
# NEVER commit real credentials to this repository.
apiVersion: v1
kind: Secret
metadata:
  name: provider-secrets
  namespace: poc-camel
  labels:
    app: poc-camel
    component: secrets
type: Opaque
stringData:
  provider.a.api.key: "changeme-provider-a-key"
  provider.a.api.secret: "changeme-provider-a-secret"
  provider.b.api.key: "changeme-provider-b-key"
  provider.b.api.secret: "changeme-provider-b-secret"
  kafka.sasl.username: "changeme-kafka-user"
  kafka.sasl.password: "changeme-kafka-password"