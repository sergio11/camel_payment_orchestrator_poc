apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-processor
  namespace: poc-camel
  labels:
    app: payment-processor
    component: processor
    version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-processor
  template:
    metadata:
      labels:
        app: payment-processor
        component: processor
        version: "1.0.0"
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/q/metrics"
    spec:
      serviceAccountName: poc-camel-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: payment-processor
          image: poc-camel/payment-processor:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
          env:
            - name: QUARKUS_HTTP_HOST
              value: "0.0.0.0"
            - name: QUARKUS_HTTP_PORT
              value: "8080"
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: kafka.bootstrap.servers
            - name: QUARKUS_LOG_CONSOLE_JSON
              value: "true"
            - name: QUARKUS_LOG_CONSOLE_FORMAT
              value: "%d{yyyy-MM-dd HH:mm:ss} %-5p traceId=%X{traceId} spanId=%X{spanId} [%c{1.}] %s%e%n"
            - name: JAEGER_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: jaeger-config
                  key: jaeger.endpoint
            - name: JAEGER_SERVICE_NAME
              value: "payment-processor"
            - name: JAEGER_SAMPLER_TYPE
              value: "const"
            - name: JAEGER_SAMPLER_PARAM
              value: "1"
            - name: OTEL_JAVA_AGENT_ENABLED
              value: "true"
            - name: OTEL_EXPORTER_JAEGER_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: jaeger-config
                  key: jaeger.endpoint
            - name: OTEL_SERVICE_NAME
              value: "payment-processor"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.name=payment-processor,deployment.environment=dev"
          envFrom:
            - configMapRef:
                name: app-config
            - configMapRef:
                name: fraud-rules-config
            - configMapRef:
                name: jaeger-config
            - secretRef:
                name: provider-secrets
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health/started
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - payment-processor
                topologyKey: kubernetes.io/hostname